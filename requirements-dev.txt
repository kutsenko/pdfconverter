# Development and Testing Dependencies

# Base requirements
-r requirements.txt

# Testing framework
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0

# Code quality
black==24.10.0
ruff==0.8.4  # Modern linter (replaces flake8, pylint)
flake8==7.0.0  # Legacy linter (kept for compatibility)
pylint==3.0.3  # Legacy linter (kept for compatibility)
mypy==1.13.0
pip-audit==2.10.0  # Security vulnerability scanner
pre-commit==4.0.1  # Pre-commit hook framework

# Additional testing tools
httpx==0.26.0  # For async HTTP testing
faker==22.0.0  # For generating test data
freezegun==1.4.0  # For mocking datetime
